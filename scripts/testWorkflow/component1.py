def container_main(data, send):
    print("Component 1 is called with data: ", data)
    # send(data)
    return "Component 1 received the request"
    # while True:
    #     send({ "name" : "component1" })

